{
  "name": "qa-automation-project",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "setup": "playwright test tests/auth.setup.js --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}